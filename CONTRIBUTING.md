# 贡献指南

感谢你对「鉴史决策」案例库的兴趣。这个库的质量核心在于：**决策结构真实、选项真实、结果可追踪、引文逐字可核**。请先读完本指南再动笔。

## 一、选案标准（四条全过才收）

1. **抉择时刻明确**：当事人清楚地站在一个可辨认的分岔点上（而不是一段模糊的人生叙事）；
2. **选项真实存在过**：`options` 里的每个选项都是当事人当时**真的可以选**的，不做事后诸葛亮的虚构选项；
3. **结果可追踪到终局**：短期结果 + 终局都有史料依据，不接受「后来他过得不错」式的含混；
4. **有可引的原文**：至少 1 条逐字引文，出处精确到「书名·卷/篇名」。

## 二、数据规范

- 按 [schema/case.schema.json](schema/case.schema.json) 编写 JSON，一个案例一个文件，**文件名 = id**（如 `shiji-hanxin-kuaxia.json`）。
- id 前缀必须与底本对应：`shiji-*`（史记）、`tongjian-*`（资治通鉴）。新底本请先开 issue 讨论。
- `kernel` 从 [13 类场景内核](docs/decision-kernels.md) 中选 1–3 个，**第一个是主内核**。
- **引文必须逐字准确**（以中华书局点校本为参照），禁止凭记忆转写大意后当引文；拿不准就先不写引文，开 PR 时标注「引文待核」。引用审校注记（版本/系年存疑）可以写入 `choice` 或 `reasoning`，但引文本身不许注水。
- `paired_cases` 强烈建议填写：结果相反或同构的案例 id（会在检索时作为对照案例自动补出）。
- `risks` 字段是本库的良心所在：必须回答「这个古法迁到现代哪里会失灵」，包括幸存者偏差与适用边界。

## 三、写作风格

- 八段式（局面→选项→选择→判断→结果→原文→启发→风险），对应 schema 字段；
- `situation` 说清**约束、筹码、时间窗口**三要素；
- `reasoning` 区分「明说的理由」与「可推知的判断」，推知部分要交代依据；
- 不用现代网络语汇写古代叙事；`keywords` 里则**应该**放现代词汇（这是检索用的）；
- 篇幅参考现有案例（每案 JSON 约 3–5KB），宁可深拆一个案例，不要薄摊十个。

## 四、提交流程

```bash
# 1. fork & clone，新建分支
git checkout -b case/shiji-xxx

# 2. 写好 cases/shiji-xxx.json 后，本地校验（零依赖，Python 3.7+）
python3 scripts/validate.py

# 3. 冒烟测试：确认新案例能被检索到
python3 scripts/advisor.py "一个贴近该案例的现代处境描述"

# 4. 提交 PR
```

若仓库已启用 CI（`.github/workflows/ci.yml`，见工作区模板，可在 GitHub 网页端添加启用），
PR 会自动运行校验器与引擎冒烟测试，全绿后进入人工审校（重点审引文与选项真实性）。

## 五、修 bug / 改引擎

- 引擎保持**零第三方依赖**（Python 3.7+ 标准库），这是硬约束；
- 改动 `KERNEL_LEXICON` 或打分逻辑时，附上 3 个以上查询的前后对比（查询 → 命中案例）；
- 一切改动先跑 `python3 scripts/validate.py`。
