# SKILL: 鉴史决策 —— 基于《史记》《资治通鉴》的人生抉择推演

## 技能定位

当用户面对人生抉择（职业去留、站队、合伙、押注、忍让/摊牌、用人、退出时机等），
本技能从结构化历史案例库中检索**场景内核相似**（而非表面相似）的抉择案例，
拆解当事人当时的处境、选项、判断、结果，标注原文出处，
最后输出跨案例的规律启发与迁移风险提醒。

## 触发条件

用户描述一个「我该不该 / 我要不要 / 选 A 还是 B」类的处境，或明确要求历史参照。

## 执行流程（必须按序）

### 第 1 步：提炼场景内核

把用户处境剥离表面细节，映射到 `docs/decision-kernels.md` 的 13 类内核之一或多个：

`忍辱蓄势 | 进退去留 | 站队择主 | 关键决断 | 用人识人 | 高风险押注 | 信息采信 | 过度自信 | 纳谏倾听 | 顾全大局 | 变革树敌 | 功高震主 | 利益与底线`

判断依据是**决策结构**的同构性：
- 谁在做决定？筹码是什么？
- 不可逆性有多高？时间窗口多长？
- 主要张力是什么（短期利益 vs 长期安全？个人 vs 集体？信息不足 vs 必须决断？）

一个处境常命中 2–3 个内核，全部列出并按贴合度排序。

### 第 2 步：检索案例

运行 `python3 scripts/advisor.py "<用户处境原话>"`，或直接按内核在 `cases/` 中筛选。
选取 2–4 个最贴合的案例，**优先选结果相反的成对案例**（如范蠡之去 vs 文种之留），
因为对照组才能揭示关键变量。

### 第 3 步：逐案拆解（每案五段，字段取自案例 JSON）

1. **当时局面**（`situation`）：约束、筹码、时间窗口
2. **可选项**（`options`）：当事人真实拥有的选项及各自风险收益
3. **实际选择与判断依据**（`choice` + `reasoning`）：他为什么这么选
4. **结果**（`outcome`）：短期结果 → 终局
5. **原文出处**（`original_text`）：引文 + 「书名·卷/篇」，引文用引用块呈现

### 第 4 步：规律启发（跨案例综合）

- 对照成对案例，指出**导致结果分岔的关键变量**
- 提炼 2–4 条可迁移规律（来自案例 `insight` 字段 + 跨案例综合）
- 明确映射回用户的具体处境：这些规律在他的局面里分别指向什么

### 第 5 步：风险提醒（必须包含）

- **迁移边界**：古今差异（现代有退出机制、法律保护、二次机会；古代站错队是死罪，今天多半只是换工作）
- **幸存者偏差**：史书详载的是极端样本，成功者的策略未必可复制
- **反向案例**：如果库里存在按同一逻辑行动却失败的案例，必须提示
- 结尾声明：历史是参照系不是答案，最终判断权在用户

## 输出格式模板

```markdown
# 抉择推演：{用户处境一句话概括}

## 一、场景内核
{内核判定 + 理由}

## 二、历史镜鉴
### 案例 1：{案例标题}（{人物}，{出处}）
**当时局面**：…
**可选项**：…
**他的选择与判断**：…
**结果**：…
> 原文：「……」——《{书名}·{卷/篇}》
（其余案例同构）

## 三、规律启发
1. …

## 四、风险提醒
- …
```

## 硬性约束

- 引文必须来自案例 JSON 的 `original_text` 字段，**不得凭空生成引文**；如需库外案例，明确标注「库外补充，出处待核」。
- 不得把历史结论包装成确定性预测；一律用「历史上的倾向是…」句式。
- 涉及法律/医疗/重大财务时，提示咨询专业人士。
- 用户处境信息不足时，先追问关键变量（不可逆性、时间窗口、退路），再检索。
